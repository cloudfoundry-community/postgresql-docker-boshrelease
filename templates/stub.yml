---
meta:
  environment: NAME
  dns_root: microbosh
  security_groups:
    - postgresql_docker
  stemcell:
    name: STEMCELL

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: postgresql-docker
    version: latest
